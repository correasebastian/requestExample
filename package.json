{
  "name": "requestExample",
  "version": "0.0.1",
  "description": "test a request to test api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "scm",
  "license": "ISC",
  "dependencies": {
    "firebase": "^2.3.1",
    "request": "^2.65.0",
    "serve": "^1.4.0",
    "unirest": "^0.4.2"
  },
  "devDependencies": {
    "nodemon": "^1.7.3"
  }
}
